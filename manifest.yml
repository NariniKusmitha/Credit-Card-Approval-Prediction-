applications:
  - name: credit-card-approval-predictor
    memory: 512M
    instances: 1
    buildpacks:
      - python_buildpack
    command: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 8 app:app
    env:
      PYTHON_VERSION: 3.10.12
